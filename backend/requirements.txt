fastapi
starlette>=0.40.0,<0.50.0
uvicorn
websockets>=13  # uvicorn's WebSocket protocol for the assistant socket (/agent/ws)
python-dotenv
qdrant-client
sentence-transformers
langchain
langchain-community
langchain-openai
langgraph
pypdf
numpy
scikit-learn
requests
httpx
jsonschema>=4
# Postgres (Supabase) backend for the stores; unused while DATABASE_URL is empty.
psycopg[binary]>=3.1
psycopg-pool>=3.1
# Lets scripts trust the OS certificate store (antivirus TLS inspection on Windows).
truststore>=0.9
datasets
ragas
PyMuPDF
python-multipart
langsmith
networkx
tree-sitter==0.25.2
tree-sitter-javascript==0.25.0

# Optional. Install to enable the anthropic provider (LLM_PROVIDER=anthropic).
# langchain-anthropic

# Optional. Install for structured PDF parsing (sections, figures, equations).
# Without it, document_structure falls back to the existing chunk pipeline.
# docling

# Optional. Install to enable ONNX model-graph verification evidence.
# onnx
