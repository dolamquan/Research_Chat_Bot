fastapi
starlette>=0.40.0,<0.50.0
uvicorn
python-dotenv
qdrant-client
sentence-transformers
langchain
langchain-community
langchain-openai
langgraph
pypdf
numpy
scikit-learn
requests
datasets
ragas
PyMuPDF
python-multipart
langsmith
networkx

# Optional. Install to enable the anthropic provider (LLM_PROVIDER=anthropic).
# langchain-anthropic

# Optional. Install for structured PDF parsing (sections, figures, equations).
# Without it, document_structure falls back to the existing chunk pipeline.
# docling

# Optional. Install to enable ONNX model-graph verification evidence.
# onnx
