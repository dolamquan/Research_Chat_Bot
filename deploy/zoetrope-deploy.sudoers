# Validate with `visudo -cf` before installing. This lets the service account
# restart exactly its own API after a deployment, and nothing else as root.
zoetrope ALL=(root) NOPASSWD: /usr/bin/systemctl restart zoetrope-api
